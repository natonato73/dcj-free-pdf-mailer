# DCJ Free PDF Mailer リリースノート

## 1. リリース概要

このドキュメントは、DCJ Free PDF Mailerの販売・納品・保守に向けた最終整理として、現時点の完成済み機能、本番確認済み内容、ファイル構成、運用上の注意、今後の改善候補をまとめるものです。

本版は、無料PDF配布フォームの作成、PDFリンク付きメール送信、送信ログ管理、お知らせ受信同意、購読者リスト管理、配信用CSV出力、簡易メルマガ送信、配信停止URL、reCAPTCHA v3によるスパム対策までを一通り扱える状態です。

導入先のWordPress環境、サーバー環境、メール送信環境、運用ルールによって、追加確認や調整が必要になる場合があります。

## 2. 完成済み主要機能

### v1.6.1 最新リリース項目

v1.6.1では、本番運用開始前の整理として、PDF設定0件時の扱いとメルマガ送信ログの管理機能を修正しました。

- PDF設定を全削除しても初期サンプルPDF設定が自動復活しないように修正
- PDF設定0件時は管理画面に「PDF設定はまだありません。新規追加してください。」と表示
- プラグイン有効化時の初期サンプルPDF自動生成を停止し、未設定時は空配列で開始
- メルマガ送信ログ付近に「送信ログをクリア」ボタンを追加
- メルマガ送信ログのみを削除し、購読者、PDF設定、メルマガテンプレート、配信カテゴリには影響しない
- プラグインVersion表記を 1.6.1 に更新

### PDF配布・PDF設定管理

- 無料PDFフォーム表示
- PDFリンク付きメール送信
- メール送信診断パネル
- PDF設定の新規登録
- PDF設定の編集
- PDF設定の削除
- PDF設定の複製
- 管理ID候補生成
- 管理ID形式 `dcj-001-ja` / `dcj-002-en`
- PDF URL / サムネイルURLのメディアライブラリ選択
- 日本語 / 英語のデフォルト文切替
- メール送信者名 / 送信元メールアドレス設定
- サイトURL、サイトドメイン、管理者メール、送信元メール情報の表示
- 送信元メールアドレスとサイトドメインの簡易チェック
- 管理画面からのテストメール送信
- SMTP / SPF / DKIM / DMARC確認を促す注意文

### v1.6.0 最新リリース項目

v1.6.0では、無料PDFごとに管理者が配信カテゴリを設定し、メルマガ同意済みの購読者へカテゴリタグを自動付与できるようにしました。

- 固定の配信カテゴリ7種類を追加
- PDF設定の追加・編集フォームに配信カテゴリの複数選択を追加
- 公開側の無料PDF登録フォームには配信カテゴリ選択欄を表示しない
- メルマガ同意ありの無料PDF申し込み時、PDF設定の `tags` を購読者データへ自動追加
- 既存購読者の `tags` は上書き削除せず、新しいタグを追加統合
- 同じタグは重複保存しない
- 購読者手動追加で配信カテゴリタグを選択可能
- 購読者一覧に配信カテゴリ列を追加し、日本語ラベルで表示
- 購読者CSVテンプレート、インポート、各種エクスポートに `tags` 列を追加
- CSVの `tags` は `new_pdf|seasonal|kids` のような `|` 区切りで扱う
- CSVインポートの `tags` は、日本語タグ名と英語tagIDのどちらでも入力可能
- CSVインポート画面に `tags` の `|` 区切り説明と対応表を表示
- CSVインポートの日本語タグ名は、空白混入や一部表記ゆれも英語tagIDへ変換
- メルマガ送信フォームに配信カテゴリの複数選択を追加
- メルマガ送信対象は、言語条件に加えて、選択カテゴリのいずれかを持つ購読者へOR条件で絞り込み可能
- メルマガプレビューの対象人数に配信カテゴリ条件を反映し、選択カテゴリ名も表示
- 本送信時もプレビュー時と同じ配信カテゴリ条件を使用
- メルマガテンプレートの保存、呼び出し、編集、上書き保存、複製で `tags` を維持
- プラグインVersion表記を 1.6.0 に更新
- DBテーブル追加、予約配信、HTMLメールエディタ、画像添付、外部メール配信サービス連携は対象外

### v1.5.2 最新リリース項目

v1.5.2では、既存テンプレートを元に別テンプレートを作れるように、メルマガテンプレート複製機能を追加しました。

- 保存済みテンプレート一覧に「複製」ボタンを追加
- 複製元の送信対象、件名、本文をコピーして新しいテンプレートとして保存
- 複製後のテンプレート名は元のテンプレート名の末尾に「 コピー」を付与
- 複製元テンプレートは変更しない
- 複製操作では本送信、購読者DB更新、送信ログ追加は行わない
- プラグインVersion表記を 1.5.2 に更新

### v1.5.1 リリース項目

v1.5.1では、保存済みメルマガテンプレートを削除して作り直さずに調整できるよう、テンプレート編集・上書き保存機能を追加しました。

- 保存済みテンプレート一覧に「編集」ボタンを追加
- 編集時にテンプレート名、送信対象、件名、本文をフォームへ読み込み
- 編集中のテンプレート名をフォーム付近に表示
- 編集中は保存ボタンを「テンプレートを更新」に変更
- 同じテンプレートIDのまま上書き保存可能
- 編集キャンセルで新規保存状態へ戻れる導線を追加
- 編集、上書き保存、編集キャンセルでは本送信、購読者DB更新、送信ログ追加は行わない
- プラグインVersion表記を 1.5.1 に更新

### v1.5.0 リリース項目

v1.5.0では、よく使うメルマガ文面を再利用できるように、メルマガ本文テンプレート保存機能を追加しました。

- メルマガ送信フォームの送信対象、件名、本文をテンプレートとして保存可能
- 保存項目はテンプレート名、送信対象、件名、本文、更新日時
- 保存済みテンプレートを管理画面に一覧表示
- 一覧から「このテンプレートを使う」で送信フォームへ内容を反映可能
- 一覧からテンプレート削除が可能
- テンプレート保存先はWordPress optionとし、DBテーブル追加は行わない
- テンプレート呼び出し、削除では実際の送信、購読者DB更新、送信ログ追加は行わない
- プラグインVersion表記を 1.5.0 に更新
- 予約配信、HTMLメールエディタ、画像添付、外部メール配信サービス連携は対象外

### v1.4.1 リリース項目

v1.4.1では、v1.4.0で追加したメルマガ送信MVPに対して、本番運用時の誤送信防止と操作性を小さく改善しました。

- メルマガ送信プレビュー表示中に「プレビューをクリア」ボタンを追加
- プレビュークリアで現在のプレビューだけを削除し、通常入力状態へ戻れるように改善
- プレビュークリアでは購読者DB更新、送信ログ追加、本送信処理は行わない
- プレビュー表示中、送信実行ボタン付近に本送信前の注意文を追加
- 対象購読者数が10件以上の場合に追加の注意表示を追加
- プラグインVersion表記を 1.4.1 に更新
- DB構造、予約配信、テンプレート保存、外部メール配信サービス連携は変更なし

### v1.4.0 リリース項目

v1.4.0では、購読者向けの簡易メルマガ送信機能をMVPとして追加しました。

- 管理画面に「メルマガ送信」セクションを追加
- 送信対象は全員、日本語購読者、英語購読者から選択可能
- 件名、本文、テスト送信先メールアドレスを入力可能
- 本送信前のテスト送信に対応
- 本送信前のプレビューと対象購読者数の確認に対応
- プレビュー後のみ送信実行できる安全導線を追加
- wp_mail による手動送信に対応
- 送信日時、件名、送信対象、対象件数、成功件数、失敗件数を簡易ログとして保存
- メルマガ送信ログはWordPress optionに保存し、DBテーブル追加は行わない
- 予約配信、HTMLメールエディタ、画像添付、自動配信、外部メール配信サービス連携は対象外

### v1.3.2 リリース項目

v1.3.2では、CSVインポートのプレビュー後に確認内容を取り消して、通常状態へ戻れる導線を追加しました。

- CSVインポートのプレビュー後に「プレビューをクリア」ボタンを追加
- プレビュー結果をクリアして、別のCSVファイルを選び直しやすく改善
- プレビュークリアではデータベース更新を行わない
- 既存のCSVプレビュー処理、CSVインポート実行処理、DB構造は変更なし

### v1.3.1 リリース項目

v1.3.1では、v1.3.0本番適用済みの安定版に対して、購読者CSVインポート周りと管理画面表示を小さく改善しました。

- プラグインVersion表記を 1.3.1 に更新
- 購読者CSVインポート用テンプレートのダウンロード導線を追加
- CSVインポート画面に、UTF-8推奨、email必須、lang推奨値、status推奨値、プレビュー確認の説明を追加
- 手動追加・CSVインポート周りの成功、失敗、注意メッセージを初心者向けに微調整
- 言語が未設定の購読者がある場合、購読者リスト上部に注意表示を追加
- DB構造、CSVインポートの基本処理、配信停止処理は変更なし
- 販売資料更新、PDFマニュアル化、zip作成、GitHub Release作成は対象外

### v1.3.0 リリース項目

v1.3.0では、管理画面説明文の整理、購読者の手動追加、購読者CSVインポートを追加しました。

- 管理画面説明文まとめ改善
- 購読者の手動追加を実装
- 管理画面の購読者リストから、同意確認済みのメールアドレスを1件ずつ追加可能
- 入力項目はメールアドレス、言語、登録元メモ、同意確認済みチェック
- 手動追加の言語は `ja` または `en` を必須にし、言語未指定の新規登録は許可しない
- 既に登録済み、または配信停止済みのメールアドレスは追加不可
- 配信停止済みアドレスを自動で購読中に戻さない
- 手動追加した購読者は、既存の購読者リスト表示とCSV出力に同じ形式で反映
- 手動追加フォーム付近に成功・エラー表示を追加
- 購読者CSVインポート仕様メモを作成
- 購読者CSVインポートを実装
- CSVファイル選択、2つの確認チェック、プレビュー、インポート実行に対応
- CSV形式はヘッダー行あり、必須列 `email` / `lang`、任意列 `source_note` / `consent_confirmed`
- CSVインポートの `lang` は `ja` または `en` を必須にし、言語未指定の新規登録は許可しない
- `consent_confirmed` は `yes` / `1` / `true` を同意済みとして扱う
- プレビューで読み込み件数、登録予定件数、スキップ件数、エラー一覧を確認可能
- CSVインポート実行で登録成功件数、スキップ件数、エラー件数を表示
- 既に登録済み、配信停止済み、CSV内重複、不正メール、言語未指定、言語不正、同意未確認の行はスキップ
- CSVインポートした購読者は、既存の購読者リスト表示とCSV出力に同じ形式で反映
- DB構造、既存CSV出力処理、配信停止URL処理は変更なし
- 既存データの言語未指定は後方互換として残しつつ、手動追加とCSVインポートでは新しい言語未指定データを増やさない

### v1.2.2 主な追加項目

- 管理画面冒頭の説明文を整理
- 開発中の段階表現を削除
- 送信ログCSVの説明文を簡潔化
- 購読者リストの管理・バックアップ用CSV説明を整理
- 管理画面の案内文を販売・納品後の運用向けに改善

### v1.2.1 主な追加項目

- 購読者リスト周りのCSV出力ボタン名を整理
- **購読者リストをCSV出力** を **管理・バックアップ用CSV出力** に変更
- **配信用CSV出力** を **メール配信用CSV出力（全言語）** に変更
- 管理・バックアップ用CSVとメール配信用CSVの違いを説明文で明確化
- メール配信用CSVを全言語、日本語のみ、英語のみで出力できるように改善
- メール配信用CSVのファイル名に `all` / `ja` / `en` を付与
- メール配信用CSVは購読中のみを出力し、配信停止者を除外

### v1.2.0 主な追加項目

- 配信用CSV出力を追加
- 購読中のみを対象にしたCSVを出力
- 配信停止者を除外
- メール配信サービスへの手動インポートを補助
- 全言語・日本語のみ・英語のみの配信用CSV出力に対応
- 既存の購読者リストCSVは管理・バックアップ用として維持

### v1.1.0 主な追加項目

- メール送信診断パネルを追加
- サイトURL、サイトドメイン、管理者メール、送信元メール情報を表示
- 送信元メールアドレスとサイトドメインの簡易チェックを追加
- 管理画面からテストメール送信が可能
- SMTP / SPF / DKIM / DMARC確認を促す注意文を追加

### メール本文置換タグ

- `{{title}}`
- `{{pdf_url}}`
- `{{terms_text}}`
- `{{unsubscribe_url}}`
- `{{newsletter_unsubscribe_block}}`

`{{newsletter_unsubscribe_block}}` は、お知らせ受信に同意した場合だけ配信停止案内を表示します。同意なしの場合は空欄になります。

既存PDF設定で `{{newsletter_unsubscribe_block}}` を使う場合は、メール本文へ手動追加が必要です。

### 送信ログ・CSV

- 送信ログ保存
- 送信ログ検索・絞り込み
- 送信ログCSV出力
- 検索・絞り込み条件のCSV出力反映
- 送信ログ全削除
- 送信ログ0件時は全削除ボタン非表示
- お知らせ同意あり / なしを送信ログに保存

### 購読者管理

- お知らせ受信同意チェック
- 購読者リスト管理
- 購読者検索・絞り込み
- 購読者の手動追加
- 購読者CSVインポート
- 購読者リストCSV出力
- 配信用CSV出力
- 購読中のみの配信用CSV出力
- 全言語・日本語のみ・英語のみの配信用CSV出力
- メール配信サービスへの手動インポート補助
- 検索・絞り込み条件のCSV出力反映
- 購読者ステータス管理
- 購読中 / 配信停止の切替
- 購読者の個別削除
- 配信停止URL
- 配信停止後の再同意で購読中に戻る

### スパム対策・保守性改善

- reCAPTCHA v3
- reCAPTCHA無効状態でも従来どおり送信可能
- `includes/` 配下への補助処理分離

## 3. 本番確認済み内容

以下の内容は、本番環境で基本動作確認済みです。

- 無料PDF送信
- メール送信診断パネル表示
- テストメール送信
- reCAPTCHA有効状態
- reCAPTCHA無効状態
- 配信停止URL
- `{{newsletter_unsubscribe_block}}`
- 同意ありの場合だけ配信停止案内表示
- 同意なしの場合は配信停止案内なし
- 送信ログCSV
- 購読者CSV
- 配信用CSV
- 購読者検索・絞り込み
- 購読者ステータス変更
- 購読者削除
- `includes/` フォルダを含むzipでの本番更新

## 4. ファイル構成

主な構成ファイル:

- `dcj-free-pdf-mailer.php`
- `includes/class-dcj-fpm-admin-notices.php`
- `includes/class-dcj-fpm-csv-exporter.php`
- `includes/class-dcj-fpm-recaptcha.php`
- `includes/class-dcj-fpm-unsubscribe.php`
- `includes/class-dcj-fpm-subscriber-helper.php`
- `includes/index.php`

`includes/` 配下には、管理画面メッセージ表示、CSV出力補助処理、reCAPTCHA v3判定・検証処理、配信停止URL処理、購読者補助処理を分離しています。

このファイル分割は、動作仕様を変えずに保守性を高めるための整理です。

## 5. 同梱ドキュメント

- `README.md`
- `README-ja.md`
- `docs/development-summary.md`
- `docs/operation-manual.md`
- `docs/operation-manual-ja.md`
- `docs/release-checklist.md`
- `docs/smtp-setup-notes.md`
- `docs/product-package-plan.md`
- `docs/release-notes.md`

配布zipには、`README.md`、`README-ja.md`、`docs/`、`includes/` を含めてください。`.git` と `.codex` は含めないでください。

## 6. 本番運用時の注意

- PDF URLには、ブラウザで直接開けるURLを設定してください。
- `/wp-content/uploads/dlm_uploads/` 配下など、直接アクセスが制限されるURLは避けてください。
- メール到達は、サーバー環境、DNS設定、SMTP設定、受信者側のメール環境に依存します。
- メール送信診断は簡易チェックです。メール到達を保証するものではありません。
- Gmailなどでは、サーバー経由表示や迷惑メール判定が出る場合があります。
- メールアドレス・CSVは個人情報として慎重に扱ってください。
- お知らせ配信、販売案内、クーポン案内には、購読者リストの「購読中」だけを使ってください。
- 配信停止の人には送らないでください。
- 送信ログCSVは履歴確認用です。配信用メールアドレス一覧としては使わないでください。
- 購読者リストCSVは、購読者の管理・バックアップ用として使ってください。
- 配信用CSVは、メール配信サービスへの手動インポート補助用です。全言語・日本語のみ・英語のみで出力できます。インポート前にCSV内容、同意状況、配信停止者の扱い、配信サービス側の同意管理・配信停止管理を確認してください。
- 既存PDF設定で `{{newsletter_unsubscribe_block}}` を使う場合は、メール本文へ手動追加してください。
- reCAPTCHA v3を使う場合は、本番ドメインで取得したSite Key / Secret Keyを使ってください。
- LocalWPなど登録ドメインと異なる環境では、reCAPTCHAを無効にして既存機能を確認する運用が安全です。
- 本番更新前にはバックアップを取ってください。
- 本番更新後は、管理画面、無料PDF送信、CSV出力、購読者リスト、配信停止URL、reCAPTCHAを軽く確認してください。

## 7. zip作成・配布時の注意

- zipには `includes/` フォルダを必ず含めてください。
- zipには `docs/` フォルダを含めてください。
- zipには `README.md` / `README-ja.md` を含めてください。
- zipには `.git` を含めないでください。
- zipには `.codex` を含めないでください。
- `includes/` が欠けると、PHPエラーやプラグイン停止の原因になります。

## 8. 販売・納品時に説明すべきこと

- メール到達は保証できません。
- 到達率改善のため、SMTP設定が別途必要になる場合があります。
- SPF / DKIM / DMARCなどのDNS設定が関係する場合があります。
- 導入先のWordPress環境、サーバー環境、テーマ、他プラグインとの組み合わせにより、調整が必要になる場合があります。
- WordPress本体や他プラグインとの相性確認は、導入先環境ごとに必要です。
- 個人情報の取り扱い、メール配信ルール、同意管理の運用方針は、運用者側でも確認が必要です。
- 本プラグインは、専用のメール配信サービスやCRMを置き換えるものではありません。

## 9. 今後の改善候補

- SMTP設定ガイド強化
- SPF / DKIM / DMARC 設定手順の整理
- DNS自動チェックは未実装
- reCAPTCHA設定手順の画像付きマニュアル化
- 管理画面UI整理
- PDF設定管理処理の段階的分離
- フォーム送信処理の段階的分離
- 専用DBテーブル化
- メール配信サービスAPI連携は未実装
- 配信停止情報の外部サービス同期は未実装
- WordPress / PHP バージョン対応表
- 販売用PDFマニュアル化
- テスト項目表の整備

## 10. リリースメモ欄

```text
リリース日：
コミットID：
確認環境：
本番バックアップ：
本番確認：
確認者：
メモ：
```
