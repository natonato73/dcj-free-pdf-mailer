# DCJ Free PDF Mailer

DCJ Free PDF Mailerは、WordPress上で無料PDF配布フォームを作成するためのプラグインです。

ユーザーがフォームにメールアドレスを入力すると、設定したPDFリンクをメールで送信できます。複数のPDF設定を管理でき、日本語・英語の配布導線にも対応しやすい構成になっています。

## 概要

このプラグインでは、WordPress管理画面からPDF配布用の設定を作成し、ショートコードを使って固定ページや投稿にフォームを表示できます。

主な流れは次の通りです。

1. PDF設定を作成する
2. ショートコードをページに貼る
3. ユーザーがメールアドレスを入力する
4. PDFリンク付きメールが送信される
5. 管理画面で送信ログを確認する

PDF設定はWordPressのoptionに保存されます。専用のDBテーブルは使用していません。

## 主な機能

- PDF配布フォーム作成
- ショートコード設置
- メールアドレス受付
- PDFリンク自動送信
- reCAPTCHA v3による無料PDFフォームのスパム対策
- メール本文への配信停止URLタグ `{{unsubscribe_url}}` 挿入
- 同意ありの場合だけ配信停止案内を表示する `{{newsletter_unsubscribe_block}}` に対応
- PDF設定の新規登録・編集・削除
- PDF設定の複製
- 管理ID候補生成
- PDF URL / サムネイルURLのメディアライブラリ選択
- 日本語・英語デフォルト文切替
- お知らせ受信同意チェック
- 送信者名・送信元メールアドレス設定
- 送信ログ保存
- 送信ログの検索・絞り込み
- 検索・絞り込み条件をCSV出力へ反映
- 送信ログ・CSVへの同意有無保存
- 送信ログCSV出力
- 送信ログ全削除
- 購読者リスト管理
- 購読者リストの検索・絞り込み
- 購読者リストCSV出力
- 購読者ステータス管理
- 購読中 / 配信停止の切替
- 購読者の個別削除

## 想定用途

- 無料PDFサンプル配布
- 塗り絵PDF配布
- 教材PDF配布
- 小規模サイトのメール取得導線
- KDP / Etsy / Gumroad などへの販売導線補助

メールアドレスを受け取り、PDFリンクを案内するシンプルな配布導線に向いています。

## ショートコード例

```text
[dcj_free_pdf id="dcj-001-ja"]
```

`id` には、管理画面で作成したPDF設定の管理IDを指定します。

## 基本的な使い方

1. 配布したいPDFをWordPressのメディアライブラリにアップロードします。
2. WordPress管理画面で **DCJ Free PDF** を開きます。
3. PDF設定を新規作成します。
4. PDF URLを設定します。
5. メール件名・メール本文を確認します。
6. ショートコードを固定ページや投稿に貼ります。
7. テスト送信します。
8. 送信ログを確認します。

初めて使う場合は、公開前に自分宛てのテスト送信を行うことをおすすめします。

## PDF URLの注意

PDF URLには、ブラウザで直接開けるURLを指定してください。

避けた方がよい例:

```text
/wp-content/uploads/dlm_uploads/
```

上記のような直接アクセスが制限される場所のURLでは、メール内のPDFリンクが `Forbidden` になる場合があります。

PDFリンクが開けない場合は、通常のWordPressメディアライブラリにPDFをアップロードし、そのURLを使ってください。

## メール到達に関する注意

このプラグインはWordPressの `wp_mail()` を使ってメール送信します。

メールの到達状況は、サーバー環境、DNS設定、SMTP設定、受信者側のメール環境などに影響されます。

起こりうること:

- メールが迷惑メールフォルダに入る
- Gmailなどでサーバー経由表示になる
- 受信者側の設定によりメールが見つかりにくい

到達率を改善したい場合は、WP Mail SMTPやFluentSMTPなどのSMTP設定を検討してください。SPF / DKIM / DMARCなどのDNS設定が関係する場合もあります。

## 動作環境

- WordPress
- PHP
- WordPress管理者権限

詳細な対応WordPressバージョン、PHPバージョンは今後整理予定です。

## インストール方法

1. プラグインzipを用意します。
2. WordPress管理画面を開きます。
3. **プラグイン** > **新規追加** を開きます。
4. **プラグインのアップロード** を選択します。
5. zipファイルを選択してインストールします。
6. プラグインを有効化します。
7. **DCJ Free PDF** 管理画面を開きます。
8. PDF設定を作成します。

## ファイル構成と配布zipの注意

このプラグインは、メインファイル `dcj-free-pdf-mailer.php` と、`includes/` 配下の補助クラスで構成されています。

`includes/` には、管理画面メッセージ表示、CSV出力補助処理、reCAPTCHA v3判定・検証処理、配信停止URL処理、購読者補助処理、直接アクセス防止用の `includes/index.php` が含まれます。ファイル分割は動作仕様を変えるためではなく、保守しやすくするための整理です。

配布zipを作成するときは、`includes/` フォルダを必ず含めてください。`.git` と `.codex` は配布zipに含めないでください。

## 同梱ドキュメント

- `docs/operation-manual-ja.md`
- `docs/operation-manual.md`
- `docs/release-checklist.md`
- `docs/smtp-setup-notes.md`
- `docs/product-package-plan.md`
- `docs/development-summary.md`

日本語で操作方法を確認したい場合は、まず `docs/operation-manual-ja.md` を参照してください。

## 開発状況

現在はカスタム開発プラグインとして整備中です。

WordPress.org公式ディレクトリ公開用ではありません。将来的な販売・納品・保守を見据えて、機能とドキュメントを整理しています。

現在確認済みの主な機能:

- 新規登録
- 編集更新
- 削除
- 複製
- ID候補生成
- メディアライブラリURL選択
- 日本語・英語デフォルト文切替
- メール送信
- PDFダウンロード
- reCAPTCHA v3検証
- 送信ログ保存
- 送信ログの検索・絞り込み
- 検索・絞り込み条件をCSV出力へ反映
- お知らせ受信同意チェック
- 送信ログ・CSVへの同意有無保存
- 送信ログCSV出力
- 送信ログ全削除
- メール送信者設定
- 購読者リスト管理
- 購読者リストの検索・絞り込み
- 購読者リストCSV出力
- 購読中 / 配信停止の切替
- 購読者の個別削除
- メール本文の `{{unsubscribe_url}}` による配信停止URL表示
- メール本文の `{{newsletter_unsubscribe_block}}` による同意あり向け配信停止案内表示

## 注意事項

- メール到達は保証できません。
- PDF URLは必ず事前に開いて確認してください。
- PDF送付とお知らせ配信は分けて考えてください。
- お知らせや販売案内を送る場合は、同意ありのメールアドレスだけを使ってください。
- お知らせや販売案内を送る場合は、購読中のメールアドレスだけを使ってください。
- 送信ログCSVはフォーム送信履歴の確認用です。配信用メールアドレスは、購読者リストでステータスを「購読中」に絞り込んでCSV出力してください。
- 配信停止の人には送らないでください。
- 配信停止URLは、メール本文に `{{unsubscribe_url}}` を入れた場合にURL単体として送信されます。
- 通常は、お知らせ受信に同意した場合だけ案内を表示できる `{{newsletter_unsubscribe_block}}` の利用が自然です。既存PDF設定のメール本文には自動追加されないため、使いたい場合は手動で追加してください。
- 配信停止URLをクリックした人は購読者リスト上で配信停止になりますが、無料PDFの受け取りには影響しません。
- reCAPTCHA v3を使う場合は、Google reCAPTCHA管理画面でv3のSite KeyとSecret Keyを取得し、実際にフォームを設置するドメインを登録してください。
- LocalWPなど登録ドメインと異なる環境では、reCAPTCHA検証が正常に通らない場合があります。ローカルではreCAPTCHAを無効にして既存機能を確認し、本番で有効化テストを行う運用が安全です。
- Score Thresholdは通常0.5から開始し、正常な送信が失敗する場合は0.3などに下げて確認してください。
- 送信ログCSVと購読者リストCSVには、管理画面の検索・絞り込み条件が反映されます。
- メールアドレスやCSVは個人情報として慎重に管理してください。
- 購読者CSVやメールアドレスは慎重に管理してください。
- 本番導入前にはバックアップを取ってください。
- SMTP情報、パスワード、APIキーなどはGitHubやドキュメントに保存しないでください。
- 他のテーマやプラグインとの組み合わせにより、調整が必要になる場合があります。

## ライセンス

ライセンスは未定です。

将来的な販売・配布方針に合わせて決定予定です。
